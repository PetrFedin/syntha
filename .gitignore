# Backups and logs
*.bak.tsx
*.old
*.tmp
*.log
dev_server.log
brace_balance.txt

# Build artifacts (if not already present)
.next/
out/
build/
coverage/
htmlcov/

# Next кастомные dist-dir (synth-1-full), не путать с корневым .next/
_ai-share/synth-1-full/.next-codex/
_ai-share/synth-1-full/.next-isolated/

# Python
__pycache__/
*.py[cod]
*$py.class
*.pyc
.Python

# Generated / AI artifacts
.ai_reports/
.ai_context/
node_modules/

# Local secrets and dev DB (do not commit)
.env
.env.local
synth1.db

# Локальные зеркала веток / копии full (не канон, легко раздувают репо при случайном git add)
.worktrees/

# Старый корень Next при появлении у кого-то локально (субмодуль из монорепо удалён)
synth-1/

# Renova — отдельный продукт (github.com/PetrFedin/renova), не часть Syntha
renova/
renova-os/

