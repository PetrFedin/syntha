'use client';

import BrandsDirectoryPage from '@/app/admin/brands/page';

/** Короткий URL `/factory/brands` — тот же экран, что `/factory/production/brands`. */
export default function FactoryBrandsPage() {
  return <BrandsDirectoryPage />;
}
