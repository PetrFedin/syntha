'use client';

type Props = {
  className?: string;
};

/** Deprecated: demo/offline в Platform Core работает без operator-панели PG. */
export function PlatformCorePgUnavailablePanel(_props: Props) {
  return null;
}
