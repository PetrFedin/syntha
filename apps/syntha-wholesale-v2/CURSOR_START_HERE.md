# Cursor Start Here — Syntha Wholesale V2

Workspace: `apps/syntha-wholesale-v2`.

Read in order:
1. `AGENTS.md`.
2. The exact task.
3. `docs/architecture/context-map.json`.
4. The affected module `README.md`, `public.ts`, exact specs and nearest tests.

Implement one vertical slice. Cross-module imports use `public.ts`. Run relevant checks. Never report an unrun check as passed.
